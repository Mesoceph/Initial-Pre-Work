//This lesson introduces the Math.random() function that returns a random number between 0 (inclusive) and 1 (exclusive).

function randomFraction() {

  // Only change code below this line.

  return Math.random();

  // Only change code above this line.
}
