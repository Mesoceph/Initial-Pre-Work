//This lesson covers the augmented multiplication operator, which works in the same way as the augmented addition and subtraction operators.

var a = 5;
var b = 12;
var c = 4.6;

// Only modify code below this line

a *= 5;
b *= 3;
c *= 10;
