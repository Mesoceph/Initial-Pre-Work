//This lesson introduces the strict inequality operator (!==), which is the opposite of the strict equality operator.  If the two values are not strictly equal, then it returns a value of true.

// Setup
function testStrictNotEqual(val) {
  // Only Change Code Below this Line

  if (val !== 17) {

  // Only Change Code Above this Line

    return "Not Equal";
  }
  return "Equal";
}

// Change this value to test
testStrictNotEqual(10);
