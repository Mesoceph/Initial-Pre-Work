//This lesson introduces the parseInt function, which parses a string and returns an integer value.  If the function cannot convert the first character to a number, the function returns NaN (not a number).

function convertToInteger(str) {
  return parseInt(str);
}

convertToInteger("56");
