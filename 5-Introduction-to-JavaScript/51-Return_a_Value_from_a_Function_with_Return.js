//In this lesson, I learned how to use a return statement to retrieve a value produced by a function.  The value from a return statement can be assigned to a variable to store the results.

// Example
function minusSeven(num) {
  return num - 7;
}

// Only change code below this line

function timesFive(num) {
  return num * 5;
}

console.log(timesFive(0));
console.log(timesFive(5));
